# 复制本文件为 .env 后按需修改（.env 已被 .gitignore 忽略，不会提交）
# 服务端口
PORT=3000
# JWT 密钥：生产环境务必改成随机长字符串
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRES_IN=24h
# 运行环境 development | production
NODE_ENV=development
# SQLite 数据库路径（相对路径相对 server/ 目录解析）
DB_PATH=./supermarket.db
